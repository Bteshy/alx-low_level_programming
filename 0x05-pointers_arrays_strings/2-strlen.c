#include "main.h"
/**
* swap_int -swap integer values
*
*@a: first value
*@b: second value
*/
void swap_int(int *a, int *b)
{
	int len = 0;
	
	while (*s != '\0') 
		{
			len++;
			s++;
		}
    return length;
}
