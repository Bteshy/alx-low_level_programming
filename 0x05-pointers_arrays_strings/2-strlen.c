#include "main.h"
/**
 * _strlen - gets the length of a string
 *
 * @s: char
 * Return: length of string
 */
int _strlen(char *s);
{
	int len = 0;
	
	while (*s != '\0') 
		{
			len++;
			s++;
		}
    return length;
}
