#include <stdio.h>

/**
 * main - Entry point
 * Return:0
 */
int main(void)
{
	int n = 0;
  char l = 'a';
  
  while (n <= 9)
    {
      putchar (48 + n);
      n++;
    }

  while (l <= f)
    {
      putchar (l);
      l++;
    }
	
putchar('\n');
return (0);
}
